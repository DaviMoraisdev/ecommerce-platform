# Dívida Técnica e Pendências — Projeto E-Commerce

Registro de decisões conscientes de adiamento, avaliadas nos code reviews e agendadas para o momento arquiteturalmente correto. Não são esquecimentos — são trade-offs documentados.

Organizado por **destino**. Só pendências: dívidas pagas são removidas daqui (o histórico permanece nos PRs).

Última atualização: **Fase 5 em andamento** (Blocos 1 a 5 e 6a concluídos; 6b em PR).

---

## Limites conhecidos e aceitos (com gatilho de correção)

Trade-offs aceitos cujo **gatilho** de correção está explícito — não são trabalho agendado por data.

- **Janela de crash claim-first (notification-service):** se o processo cair entre o `claim` e o `ack`, a reentrega é tratada como duplicata sem processar o efeito. Inerente ao at-least-once sem efeito transacional. **Gatilho:** quando o consumer deixar de ser stub e ganhar efeito real (e-mail/push), é obrigatório adotar efeito+claim atômicos (outbox no consumidor). Destino: **evolução do notification-service (Fase 6+/pós-MVP)**.
- **Premissa de 2 casas decimais (payment-service):** `src/domain/money.ts` assume 100 centavos por unidade monetária. Verdadeiro para BRL, falso para JPY (0 casas) e dinares (3). Seguro enquanto `Currency = 'BRL'`. **Gatilho:** suporte a segunda moeda.
- **`Number()` aceita hexadecimal e exponencial em `parseTimeout` e `parseMinutos` (payment-service):** `PAYMENT_WINDOW_MINUTES=0x10` vira 16 e `1e3` vira 1000. Documentado em teste, não corrigido: hexadecimal num `.env` não é acidente plausível, e um parser próprio só para isso é código a mais para manter. **Gatilho:** se um dos dois for endurecido, os dois vão juntos — corrigir só um cria divergência silenciosa entre parsers irmãos.
- **Porta de pagamento sem passo de autenticação adicional (3DS/SCA):** A porta PaymentProvider nao expressa autenticacao adicional — `ChargeResult` não tem campo de próxima ação (redirect, desafio). O fluxo assume cartão tokenizado e captura automática. **Gatilho:** exigência de 3D Secure, SCA (Europa) ou qualquer método que precise de interação extra do cliente. Custo: variante nova em `ChargeResult` e ajuste em todos os consumidores.

---


## Decisões e procedimentos documentados (sem trabalho pendente)

Registros de decisão — não há tarefa a fazer, apenas contexto para o futuro.

- **`diagnostics: false` no Jest e DELIBERADO (todos os serviços):** o Jest transpila sem type-check, o que deixou a suíte do payment ~3,4× mais rápida (26,1s → 7,8s). **Não reativar.** O type-check não desapareceu: saiu do Jest e virou passo próprio. Semântica atual dos comandos — `npm test` = `typecheck + test:fast` (**padrão seguro**, ~5,3s); `npm run test:fast` = só o Jest, sem type-check (loop interno, uso consciente); `npm run verify` = `test + build`; `npm run verify:integration` = `typecheck + test:integration`. Comprovado nas duas direções: com erro de tipo injetado, `npm test` falha e `test:fast` passa.
- **Duplicacao deliberada de tooling de teste entre servicos:** a guarda de banco (`tests/helpers/testDbGuard.ts`), o script de migration (`scripts/migrate-test-db.ts`) e os testes de ambos existem em **cópia idêntica** em payment, order e inventory. Dentro de cada serviço a política é única — migration e testes importam a mesma guarda. Entre serviços, é cópia. **Aceito**, pela mesma razão do `database-error.ts`: duplicar *ferramenta* entre serviços é o preço da independência, e o que é dívida é duplicar **contrato** (como `topology.ts`), onde divergência silenciosa vira incidente. Pacote compartilhado exigiria build e versionamento de monorepo — Fase 7. **Custo aceito:** correção no invólucro precisa ser aplicada em três lugares.
- **Drain de `reserved` órfão na migração do 7a:** a migration zerou `inventory.reserved` sem `reservation` (modelo antigo). Seguro por não haver pedidos reais; **com dados reais a estratégia seria backfill, não drain**.
- **Redrive da DLQ respeita o TTL do claim:** um redrive `DLQ -> fila principal` antes do TTL expirar é visto como duplicata (ack sem reprocessar). **Procedimento:** redrive só após o TTL, ou limpar `notif:evt:<eventId>` antes.

---

## Exceções de segurança aceitas (→ Fase 7)

- **Token do usuário trafega por HTTP entre payment e order.** Levantado no review do PR #52 (achado 3.2): `parseUrlDeServico` aceita `http:` e `https:`, e o `OrderClient` repassa o `Authorization` do usuário para o endereço configurado. Se HTTP for usado sem camada externa segura, o JWT pode ser interceptado. **Não corrigido exigindo HTTPS**, e o motivo é de topologia: `http://` entre serviços dentro de um cluster com mTLS de malha é o padrão normal, e exigir `https` em produção quebraria justamente a arquitetura que a Fase 7 vai montar. A decisão correta é junto do gateway/malha: ou exigir HTTPS, ou exigir e **documentar** mTLS comprovado. Mesmo raciocínio do token de serviço ADMIN acima.

- **Token de serviço ADMIN (order → inventory):** o order assina um token ADMIN com o segredo compartilhado para chamar `reserve/release`. Mitigação aceita, não dívida paga: o segredo compartilhado já permite forjar qualquer token, então usar ADMIN não amplia o raio de ataque. Correção real: identidade por serviço (mTLS/chaves assimétricas) + issuer/audience/scopes (ex.: `inventory:reserve`). (PR #41.)
- **Authz por dono/serviço da reserva:** a posse **estrutural** foi paga no 7a (reservas amarradas ao `orderId`; `release(orderId)` só toca o que é do pedido). A **autorização** ainda é incompleta: qualquer ADMIN/SELLER libera qualquer `orderId`; qualquer papel logado reserva com `orderId` arbitrário. Exposição conhecida e aceita para este estágio. Correção: auth serviço-a-serviço + `orderId` vindo de claims confiáveis.

---

## FASE 5 — fechar no Bloco 10 (dentro desta fase)




## FASE 5 — critérios herdados entre blocos


Compromissos assumidos em um bloco que **outro bloco** desta fase tem de cumprir. Antes
viviam apenas em conversa e em descrição de PR — inclusive um controle de segurança.
Mesmo ciclo das dívidas: removidos daqui quando entregues.


### Procedimento — verificacao de ciclo de vida
- **Verificacao de ciclo de vida exige binario compilado, porta livre e liveness.** Teste unitario com dependencia injetada nao cobre a FIACAO no ponto de entrada. Duas tentativas no Bloco 3a foram invalidadas: `npx`/`npm run dev` sao wrappers e o `kill` atinge o wrapper, nao o processo; e um orfao de execucao anterior na mesma porta faz o `curl` responder por outro processo. Procedimento: `npm run build` + `node dist/server.js`, conferir que a porta estava livre, checar liveness antes do `curl`, e so entao enviar o sinal.

### Bloco 4 - Webhook e inbox

As cinco entradas abaixo foram PAGAS no PR do Bloco 4. Cada uma registra o
caso que a cobre e a sabotagem que prova que o caso pega o defeito: remover o
mecanismo derruba aquele caso, e somente ele. A REMOCAO destas linhas e escopo
do PR de manutencao do Bloco 10, junto com as demais dividas pagas da fase.

- [PAGO] **Politica fail-closed do `providerCreatedAt` nulo.** Evento sem timestamp de origem vai ao inbox como `IGNORED` e nao altera estado do pagamento.
  Prova: CASO 9 em `tests/integration/webhook.integration.test.ts`; sabotagem S3 (remover a guarda) derruba somente o CASO 9. A obrigacao registrada como `it.todo` em `schema-constraints.integration.test.ts` foi cumprida pelo CASO 9, em `webhook.integration.test.ts`; o `it.todo` foi substituido por um ponteiro para ele.
- [PAGO] **`express.json()` NAO alcanca a rota de webhook.** O `express.raw({ type, limit })` vive dentro do `webhookRouter`, e o router e montado antes do parser global em `src/app.ts`, para que o corpo cru nunca vaze para outra rota.
  Prova: CASO 5; sabotagem S11 (re-serializar o corpo antes de verificar a assinatura) derruba somente o CASO 5. S10 (remover a montagem) derruba todos os casos da suite, o que confirma que a rota e sustentada por toda a suite.
- [PAGO] **Cap de tamanho do `rawBody`.** `LIMITE_CORPO_WEBHOOK = 64kb`, acima dos 10kb do JSON global porque payload de provedor carrega a cobranca inteira, e abaixo do que serviria como vetor de exaustao de memoria.
  Prova: CASO 16 (413 e zero linha de inbox); sabotagem S12 (subir o teto para 10mb) derruba somente o CASO 16.
- [PAGO] **Sanitizacao em escrita** do payload do inbox e das mensagens de erro.
  Implementada como ALLOWLIST POR CAMINHO, com denylist por cima. A abordagem mudou tres vezes sob review: denylist pura deixava passar aliases PCI/SAD novos a cada rodada; allowlist por NOME era contornavel com estrutura aninhada; o modelo final compara NOME EXATO do wire numa arvore (`id`, `type`, `created_at`, `data` -> campos da cobranca), e fora dela entra o MODO ESTRUTURA, onde a forma e preservada e toda folha vira `[nao-reconhecido]`. A normalizacao vive so na denylist, que precisa ser tolerante; a allowlist e estrita. A arvore usa `Map` para nao herdar propriedades de `Object.prototype`, e a chave `__proto__` e renomeada na escrita porque nao sobrevive ao round-trip do Prisma/Postgres. Teto de profundidade 8 e de 100 itens por array. `lastError` nunca recebe a mensagem original do erro.
  Prova: CASO 17; sabotagem S13 (gravar `evento.raw` sem sanitizar) derruba somente o CASO 17.
- [PAGO] **`CANCELED` com trilha de transacao coerente** (achado 4.6 do review do PR #52). A `AUTHORIZE` que estiver em `PENDING` e fechada como `FAILED` + `failureCode = PROVIDER_CANCELED`. Sem migracao de enum: `failureCode` ja carrega a distincao, e `TransactionStatus` continua com tres valores.
  Prova: CASO 13; sabotagem S6 (nao fechar a autorizacao) derruba somente o CASO 13.

Dois defeitos que a suite adversarial encontrou ANTES de existirem em producao,
e que nao estavam na lista original do bloco:

- `podeTransicionar(X, X)` devolve `true` de proposito, porque replay nao e transicao. Sem curto-circuito quando o estado alvo ja e o atual, dois eventos DISTINTOS reportando o mesmo estado criariam uma segunda linha `CAPTURE`, e a trilha diria que o dinheiro foi capturado duas vezes. Coberto pelo CASO 7, sabotagem S1.
- Colisao no `@@unique` do inbox nao pode ser tratada como duplicata incondicional: uma linha `RECEIVED` ou `FAILED` significa que o efeito NUNCA aconteceu, e responder 200 prenderia o pagamento para sempre por uma falha transitoria. Coberto pelo CASO 8, sabotagem S2.
- A guarda `Buffer.isBuffer` na rota estava sem NENHUM teste. Descoberta pela sabotagem S9, que nao derrubou nada. Coberta pelo CASO 15.
- `payment.succeeded` nao validava o valor capturado contra o cobrado, embora o `POST /payments` faca isso em `assertValoresCoerentes`. Assinatura valida prova ORIGEM, nao COERENCIA. Coberto pelo CASO 18, sabotagem S14.
- `refund.succeeded` nao validava o reembolso contra o `capturedAmountCents` do NOSSO banco. Invariante distinta da do `fake.wire`, que so compara campos do payload entre si e nao conhece o nosso estado. Coberto pelo CASO 19, sabotagem S15.

### Bloco 5 - Integracao payment <-> order

DIVIDIDO em 5a (payment publica) e 5b (order consome), pelo mesmo motivo que a
Fase 3 dividiu o Bloco 8: um PR atravessando dois servicos com broker no meio
tem superficie demais para uma rodada de review so.

- [5a PAGO] **Payment publica `payment.captured` de forma confiavel.** Exchange proprio `payments` (topic), outbox gravada na MESMA transacao do efeito, relay com polling, publisher amqp com confirm channel e `mandatory`, e `RABBITMQ_URL` fail-closed em producao.
  `eventId` DETERMINISTICO (`payment.captured:<paymentId>`): o `@unique` vira trava de duplicata por construcao, e o consumidor do 5b ganha chave estavel.
  Mecanismos que so passaram a existir DEPOIS do review: publicacao com `mandatory` + listener `basic.return` (confirm do broker prova aceitacao no exchange, NAO roteamento para fila); deadline unico sobre `connect` + `createConfirmChannel` + `assertExchange`; listeners de `error`/`close` no canal; guarda de shutdown contra init tardia; redacao da credencial da URL em log; recusa de `RABBITMQ_URL` sem host.
  Prova (2026-08-21, commit 7cac8c8): `npm test` 637/637 em 22 suites; `npm run test:integration` 85/85 em 3 suites; `npm run build` e `npx tsc --noEmit` sem saida. Bateria de 10 sabotagens rodada com a arvore ja commitada: as 10 derrubaram exatamente o teste alvo, nenhuma passou vazia, nenhuma causou colateral, `git status --porcelain` vazio ao fim de cada uma.
- [5a DEVOLVIDO - pode esperar, com gatilho | PR proprio no order-service] **O publisher do order-service tem os mesmos defeitos 4.2, 4.4 e 4.5 corrigidos aqui.** O publisher do payment e copia declarada do dele, e as correcoes ficaram so de um lado.
  Confirmado por grep em 2026-08-21 sobre `services/order-service/src/events/publisher.ts`: zero ocorrencias de `mandatory` e de listener `'return'`; `withTimeout` envolve apenas `amqp.connect` (linha 96), com `createConfirmChannel` (100) e `assertExchange` (101) fora do deadline; existem `conn.on('error')` (103) e `conn.on('close')` (108), e nenhum `ch.on`.
  Exposicao MEDIDA (nao suposta): o order publica duas routing keys, `order.created` e `order.status_changed` (`services/order-service/src/events/topology.ts`, linhas 7-8), no exchange `orders`. O notification-service liga `order.*` no mesmo exchange (`services/notification-service/src/config/topology.ts`, linhas 3 e 6). Em topic exchange `*` casa exatamente uma palavra delimitada por ponto, e underscore nao delimita: as duas keys estao cobertas. NAO ha perda silenciosa em curso hoje.
  Risco que permanece: a fila so existe depois que o notification-service executa `assertQueue` (`src/server.ts`). O order sobe independente. Em ambiente novo, primeiro deploy ou volume do broker recriado, o order pode publicar antes de a fila ter sido declarada alguma vez; sem `mandatory` essas mensagens nao roteiam, o relay marca SENT e elas somem sem log.
  Gatilhos para promover a essencial: adicionar uma terceira routing key no order, ou provisionar ambiente novo.
  Nao entra no PR do 5a: e outro servico e merece review proprio.
- [5a DEVOLVIDO - pode esperar] **Estado global de modulo no publisher do payment.** `connection`, `channel`, `connecting`, `fechado` e `naoRoteadas` sao variaveis de modulo. Consequencia observavel: todo teste precisa de `jest.resetModules()` + import dinamico, e dois publishers no mesmo processo sao impossiveis. Refatorar para instancia injetada depois do 5b, junto com a mesma divida no order-service.
- [5a DEVOLVIDO - Fase 10] **Contrato de mensageria duplicado entre payment e order** (`topology.ts`, `publisher.ts`, `outbox.repository.ts`). Ja registrado para extracao em pacote compartilhado na Fase 10; esta rodada AGRAVOU a divida, porque as duas copias agora divergem em comportamento e nao so em localizacao.
- [5a DESCOBERTO - registrar, nao corrigir agora] **O CASO F2 prova que a falha do evento aborta o desfecho, NAO que o `enqueue` recebe o `tx`.** Como a falha e injetada no proprio `enqueue`, trocar o `tx` pelo cliente global manteria o teste verde — e em producao a linha da outbox sobreviveria ao rollback. Provar exigiria uma falha DEPOIS do enqueue e dentro da mesma transacao, que o fluxo atual nao oferece. Alternativa quando houver: extensao de Prisma injetando erro no commit.
- [5b PAGO] **Primeiro consumer do order-service.** Fila `orders.payments`, DLX fanout + DLQ, `prefetch(1)`, e inbox transacional com `eventId @unique`.
  DECISAO FECHADA: inbox em Postgres, NAO claim em Redis. Motivo: o efeito do order e escrita no Postgres, entao marca e efeito cabem na MESMA transacao e o instante intermediario nao existe. O notification usa Redis porque o efeito dele e enviar e-mail, que nao e transacional — la o Redis e a melhor opcao disponivel, nao a melhor opcao em geral. O order tambem nao tem `ioredis` nas dependencias.
  INVARIANTE mantido: linha no inbox existe se e somente se o efeito aconteceu. Desfechos sem efeito (pedido inexistente, valor divergente) DESFAZEM a transacao via erro de controle, em vez de commitar a marca — commitar faria a redentrega ser lida como duplicata e o evento morreria.
  BINDING ESTRITO `payment.captured`, nao `payment.*`: evento futuro sem handler nao roteia, e o publisher do payment (mandatory + basic.return, Bloco 5a) mantem PENDING e loga. Falha visivel na origem e melhor que silencio no destino.
  DIVERGENCIA deliberada do notification: perda de broker NAO derruba o processo. O order serve HTTP; matar a API de pedidos porque o RabbitMQ caiu troca degradacao parcial por indisponibilidade total.
  Prova: C1-C39 em unitario (traducao de resultado em acao, topologia com ordem real, contrato financeiro, classificacao de erro, teto de tentativas, ciclo de vida da sessao, resultado tardio e disposicao da mensagem) e G1-G11 em integracao contra Postgres real (`@unique`, rollback, unique parcial de `pending_compensations`, segunda captura, moeda, captura parcial e concorrencia).
- [5b DESCOBERTO - registrar] **O laco de reconexao (`agendarReconexao`) nao tem teste.** O ciclo de vida da sessao passou a ter cobertura (C25-C29: close de canal, cancelamento do consumer, falha no meio do setup, encerramento durante abertura, single-flight), mas o TIMER da reconexao em si nao. Mesma natureza da divida do `startOutboxRelay`/`stopOutboxRelay`.
- [5b DESCOBERTO - registrar] **Dois mecanismos de logging do handler nao tem teste que os pegue** (confirmado por sabotagem, nao suposto):
  (a) a sanitizacao de `acao.reason` no ponto de saida para log — nenhum teste inspeciona a linha emitida (sabotagem U6-bis: nada cai);
  (b) o `try/catch` do ramo de payload grande tem teste (C33) mas NAO tem prova por sabotagem: nao houve como quebra-lo cirurgicamente sem erro de sintaxe (U5).
  O padrao comum: sao protecoes cujo efeito e a AUSENCIA de algo (uma rejeicao que nao escapa, um dado que nao aparece em log). Testar ausencia exige observar o canal de saida, e nenhum destes o observa.
- [5b COBERTURA PARCIAL - registrar] **O descarte da aquisicao tardia e provado por COMPORTAMENTO, nao por mecanismo.** `ref.abortado` e `geracao++` se cobrem mutuamente: remover qualquer um dos dois isoladamente nao derruba o CASO C38 (sabotagens V1, V2, V3). Removendo os DOIS, ele cai (V9). A redundancia e deliberada — um fecha o recurso da aquisicao abandonada, o outro invalida os listeners dela — mas nenhum teste isola cada um.
- [5b DESCOBERTO - registrar] **Dois mecanismos do handler nao tem teste que os pegue** (confirmado por sabotagem): a contagem de tentativas sobrevivendo a decisao de DLQ (V6) e o `abortarSePreciso` logo apos o `consume` (V8). Nenhuma das duas sabotagens derruba nada.
- [5b COBERTURA PROBABILISTICA - registrar] **O CASO G11 cobre o CAS do `aplicarTransicao` apenas quando o entrelacamento acontece.** Remover o CAS (sabotagem V7) derrubou o G11 em 1 de 3 execucoes; em outra nao derrubou NADA, nem o teste de concorrencia que ja existia em `order.integration.test.ts`. As duas capturas disparadas juntas as vezes disputam e as vezes serializam. O G11 e valido — a invariante que ele fixa (uma marca por chamada que commitou) vale sob qualquer entrelacamento —, mas nao e garantia de deteccao de regressao no CAS. LICAO DE METODO, valida para o projeto todo: veredito de sabotagem tirado de UMA execucao, em codigo concorrente, nao vale; repetir ao menos tres vezes.
- [5b DESCOBERTO - registrar] **A guarda de geracao no `close` do canal nao tem teste.** Confirmado pela sabotagem T11: remover `if (geracao !== minhaGeracao) return` do listener nao derruba nada. A guarda existe para impedir que o listener de um canal ja descartado derrube a sessao nova, e nenhum teste monta duas geracoes.
- [5b DESCOBERTO - registrar] **A corrida do unique parcial de `pending_compensations` nao tem teste.** Duas transacoes concorrentes para o mesmo pedido cancelado: uma leva P2002, aborta junto com o inbox e vira requeue. O codigo trata, nenhum teste exercita concorrencia real.
- [ESSENCIAL - Fase 7 Seguranca | SISTEMICO, nao introduzido pelo 5b] **Nenhum publicador de evento e autenticado.** O consumidor aceita a mensagem pela routing key e pela estrutura do JSON; nada prova que veio do payment-service. Quem puder publicar no exchange `payments` leva um pedido a PAGO ou cria uma compensacao. NAO e especifico deste PR: `order.created -> notification` esta na main com a mesma exposicao desde a Fase 3, e o broker usa credencial unica. A correcao e de infraestrutura — credencial por servico e ACL de publicacao, ou assinatura do evento — e vale para todos os pares de uma vez.
  PRE-CONDICAO DE ATIVACAO IMPOSTA PELO CODIGO desde o PR #55: o consumidor e fail-closed em `PAYMENTS_CONSUMER_ENABLED`, que exige a string exata "true" (CASOS C40 e C41). Documentar nao protegia; a flag protege. Ligar apenas em ambiente cujo broker tenha credencial por servico e ACL restringindo a publicacao no exchange `payments` ao payment-service.
- [5b DEVOLVIDO - Bloco 8] **O teto de tentativas existe, mas vive em MEMORIA.** `decidirEntrega` conta requeues por `eventId` num Map de modulo e manda para a DLQ apos o teto (default 5), o que remove o starvation do `prefetch(1)` por erro permanente nao classificado. LIMITACOES conhecidas, todas sem correcao aqui: a contagem morre com o processo (reinicio zera); com varias replicas cada instancia tem a sua; e ela nao e coordenada com a confirmacao do proprio `nack-dlq`. A alternativa correta e contagem DURAVEL do broker — retry queue com TTL e `x-death` —, que e comportamento do broker e nao se prova com duble. Entra com o teste contra RabbitMQ real.
- [5b DEVOLVIDO - Fase 10] **`inbox_events` acumula dois ciclos de vida incompativeis.** Ele e ao mesmo tempo a trava tecnica de idempotencia e o REGISTRO FINANCEIRO por captura usado para reconciliar. Retencao suficiente para idempotencia (maior que o TTL de mensagem no broker) NAO e suficiente para reconciliacao, investigacao de cobranca ou auditoria: uma purga pode respeitar o TTL e ainda apagar a captura que uma compensacao aberta precisa. A separacao entre ledger financeiro e inbox tecnico, ou uma retencao condicionada a compensacao resolvida, tem de vir ANTES de qualquer rotina de purga. Segundo requisito, cumulativo: a janela de retencao tem de ser MAIOR que o TTL maximo de mensagem no broker — purgar antes disso reabre a janela de duplicata para redentregas posteriores a purga.
- [5b ASSIMETRIA conhecida] **O payload de `payment.captured` nao tem campo `type`.** O notification cruza `type` x routing key como defesa contra binding errado; aqui essa checagem nao existe e o binding estrito faz o papel. Adicionar `type` ao payload e mudanca no payment-service — avaliar na Fase 10, junto com a extracao do contrato para pacote compartilhado.
### Bloco 6 — Reconciliação e expiração
- **Recuperar chave de idempotencia presa em PROCESSING.** Quando `createCharge` falha de forma TRANSIENTE (timeout, 5xx), o dinheiro pode ter se movido — a resposta se perdeu, nao necessariamente o efeito. O servico deliberadamente NAO marca a chave como FAILED, porque isso liberaria nova tentativa com `attemptCount + 1` e chave de provedor nova, causando SEGUNDA COBRANCA. O custo: a chave fica presa em PROCESSING e o cliente nao consegue pagar aquele pedido ate a reconciliacao rodar. **[6b PAGO, por outro caminho]** A recomendacao acima estava ERRADA e nao foi seguida: repetir `createCharge` para descobrir se a cobranca existe usa uma operacao de ESCRITA como consulta, e a garantia de que a chave derivada devolveria a cobranca original e do provedor, nao nossa. O 6b adicionou consulta explicita a porta (`buscarCobrancaPorTentativa`) e aplica o desfecho pelo MESMO caminho do fluxo normal.
- **Definir TTL e limite de tentativas da janela de retentativa.**
- **[6a PAGO] Congelar a resposta do replay — DECIDIDO E IMPLEMENTADO.** Coluna `IdempotencyRecord.completedResponse` (JSONB), gravada na MESMA transacao do desfecho, lida no replay sem tocar no `Payment`. SEM constraint de banco nesta entrega — ver o item de expand/contract abaixo. Leitura VALIDA a forma campo a campo e reconstroi o objeto, entao propriedade extra na coluna nao vaza para a API. Prova: CASO F3 (integracao) monta chave recusada + chave bem-sucedida no mesmo pedido; unitario congela valores DIVERGENTES do `Payment` vivo, entao voltar a ler o vivo derruba tres assercoes. Registro historico da decisao antiga abaixo.
- **[HISTORICO — decisao anterior] Congelar a resposta do replay.** Levantado no review do PR #52 (achado 4.4, segunda metade). O vínculo requisição↔chave foi pago no Bloco 3 (coluna `requestFingerprint`), mas o replay ainda **lê o `Payment` vivo** em vez de devolver uma resposta congelada. Consequência: uma chave cuja tentativa foi recusada passa a devolver `CAPTURED` depois que **outra** chave efetuar tentativa bem-sucedida no mesmo pedido. Há contra-argumento real — refletir o estado atual é mais útil ao cliente em vários casos — então é decisão de produto, não defeito óbvio. Custo se adotado: coluna de resposta serializada e política de retenção. O estoque fica reservado durante toda a janela; é decisão de negócio com efeito em disponibilidade. Referência inicial: 15 min e 3 tentativas.
- **[6c PAGO] Modelo do contador de tentativas.** Hoje `attempts` conta apenas TENTATIVAS QUE FALHARAM com excecao. Desfechos retentaveis (`providerRef` ainda desconhecido, contencao no CAS) devolvem 503 sem incrementar, entao um teto baseado so nesta coluna nao controlaria esse tipo de reentrega. Definir no mesmo bloco do teto: ou separar `failedAttempts` de `deliveryAttempts`, ou basear a quarentena em idade do `receivedAt`. Levantado na 2a rodada de review (achado 4.2). **RESOLVIDO no 6c sem separar colunas:** `attempts` continua contando so a populacao que falha com EXCECAO, e a populacao `retentavel` — que nao passa pelo catch — passou a ser limitada por IDADE do `receivedAt`. A alternativa registrada aqui (separar `failedAttempts` de `deliveryAttempts`) foi descartada: exigiria migracao para medir duas coisas que ja tinham dois criterios naturais e distintos.
- **[6c PAGO] `attempts` do inbox sem teto.** Um evento que falha de forma DETERMINISTICA (bug no handler, ou dado que nunca vai validar) e reprocessado a cada reentrega do provedor. Hoje `WebhookEvent.attempts` apenas conta; nao ha quarentena nem parada. Definir teto e destino (parking/DLQ) junto com o job deste bloco, que ja vai varrer `RECEIVED` orfao e `IGNORED`. Levantado ao desenhar o handler do Bloco 4. **RESOLVIDO no 6c:** teto configuravel (`WEBHOOK_MAX_ATTEMPTS`, default 5), destino `QUARANTINED`, e a rota passa a responder 200 ao atingi-lo — o que encerra o laco de reentrega do lado do provedor, em vez de so registrar do nosso lado.
- **[6d PAGO] Ordenacao fina entre eventos de TRANSICAO (global, nao por tipo).** Coluna `Payment.lastProviderEventAt` (nullable, sem backfill: linha antiga aceita o primeiro evento que chegar, que e o comportamento de hoje). A comparacao vive no WHERE do compare-and-swap — `OR: [null, { lt: ocorridoEm }]` — e o marcador avanca na MESMA instrucao do efeito; gravar depois deixaria janela em que o proximo evento antigo ainda passaria. A releitura apos CAS perdido tambem compara, e e la que a obsolescencia vira MOTIVO para a triagem: o WHERE apenas impede a escrita, sem dizer por que. `<=` e nao `<`: dois eventos com o mesmo instante nao tem ordem entre si, e o primeiro ja aplicou. NAO se aplica ao caminho de REEMBOLSO, por decisao: la a defesa de ordenacao e o delta sobre `refundedAmountCents`, que compara VALOR — rejeitar reembolso por timestamp arriscaria nao registrar dinheiro que se moveu, se o relogio do provedor nao refletir a ordem causal. Prova (2026-09-01, commit 6f8f304): `npm run verify` 715/715; `npm run verify:integration` 105/105. CASOS U1-U4 (unitario) e 26-27 (integracao contra Postgres real); o 27 existe porque, sem contraparte, uma condicao INVERTIDA bloquearia todos os eventos e o 26 continuaria verde provando o oposto. Cinco sabotagens derrubaram o alvo. **CORRECOES da rodada de review (achados 3.1, 4.1, 4.2, 4.3):** (a) `providerCreatedAt` vem do PAYLOAD, e a assinatura prova a ORIGEM dos bytes, nao a PLAUSIBILIDADE do valor — mesma licao que o Bloco 4 registrou na coerencia monetaria. Como o campo vira marcador PERSISTENTE, um unico evento com timestamp absurdo bloquearia TODAS as transicoes seguintes daquele pagamento: negacao de servico por pagamento, terminal. Entrou portao de plausibilidade (`TOLERANCIA_DE_FUTURO_MS`, 5 min, constante e nao configuracao — e tolerancia a desvio de relogio, grandeza tecnica), SEM avancar o marcador. O desfecho e RETENTAVEL, e nao terminal, e a primeira versao errou nisso: eu alegava que reentregar nao muda o timestamp do payload — verdade e IRRELEVANTE, porque o outro lado da comparacao, `Date.now()`, muda. Um evento seis minutos a frente e implausivel agora e legitimo daqui a pouco, e responder 200 faria o provedor parar de reentregar: evento financeiro perdido para sempre por desvio de relogio. O caminho TERMINAL nao precisou de segundo limiar — e a quarentena por IDADE do Bloco 6c, e o CASO 43 prova que os dois mecanismos compoem (o `lastError` final carrega os dois motivos). Separadamente, `Invalid Date` E terminal: `getTime()` devolve NaN, toda comparacao com NaN e falsa, e sem checagem explicita o valor atravessaria os portoes e chegaria ao Prisma; reentregar nao conserta timestamp quebrado. (b) EMPATE ganhou motivo proprio: dizer "anterior" sobre dois eventos do mesmo instante grava informacao ERRADA no campo que a triagem le. (c) A precedencia entre "estado ja alcancado" e "obsoleto por tempo" passou a ser DECLARADA no codigo: a primeira vem antes, e a consequencia aceita e que, quando o evento ANTIGO vence a corrida, o marcador fica no instante dele — sem dano, porque o estado e identico. (d) A garantia e PROSPECTIVA: vale para eventos aplicados APOS a implantacao. Linha antiga aceita o primeiro evento que chegar, que pode ser um atrasado; nao ha backfill porque nao ha historico confiavel de qual evento produziu cada estado. (e) A ordenacao e GLOBAL entre eventos de TRANSICAO, nao por tipo — a redacao anterior dizia "mesmo tipo" e divergia da implementacao. Modelar por tipo seria especular sobre um provedor que ainda nao existe. Prova (2026-09-01, apos a 5a rodada): `npm run verify` 718/718; `npm run verify:integration` 112/112. Esta linha trazia `719/719` e `110/110`, que era a contagem INFLADA pela duplicacao descrita na nota de numeracao abaixo — o documento afirmava um numero que ele proprio desmente tres itens adiante, e ficou assim depois de eu JA saber que estava errado. CASOS U1-U7 e 40-47, incluindo concorrencia REAL (duas requisicoes simultaneas contra o Postgres, CASO 44) e reembolso fora da ordenacao (CASO 45). Nove sabotagens derrubaram o alvo.
- **[6d DECISAO documentada — nao ha trabalho pendente] As duas escritas de conclusao DENTRO da transacao nao tem guarda de estado, e isso e deliberado.** `encerrar`, o `catch` e o ramo retentavel usam `updateMany` com `status in (RECEIVED, FAILED)`; as escritas em `tx.webhookEvent.update` (caminho de captura e de reembolso) usam `update` por id, sem guarda. Cheguei a propor uniformizar e a analise mostrou o contrario: so se alcanca aquelas linhas tendo GANHO o compare-and-swap, ou seja, tendo aplicado o efeito financeiro — marcar PROCESSED por cima de IGNORED ou QUARANTINED e dizer a verdade. COM a guarda, o dinheiro se moveria e a linha NAO registraria isso. O comentario ficou no codigo justamente para a proxima pessoa nao 'corrigir' a assimetria e criar o defeito.
- **[6d — nota de metodo] Duble com `as` aceita objeto INCOMPLETO, e o campo faltante so aparece quando alguem o le.** O `paymentDeTeste` (`prisma-fake.ts`) foi pego pelo compilador ao ganhar `lastProviderEventAt`, porque la nao ha cast. O `pagamento()` do teste unitario do webhook usa `as Payment` e passou batido: o campo chegou como `undefined` a uma guarda escrita para `null`, e o `.getTime()` estourou. Em producao o Prisma devolve `null` e isso nao aconteceria — mas um TypeError naquele caminho vira 500, o provedor reentrega, `attempts` sobe e o evento acaba em quarentena por defeito nosso. Corrigido nos DOIS lados: a guarda deixou de depender da distincao `null`/`undefined`, e o duble deixou de mentir sobre a forma. Vale como padrao: cast em duble desliga a verificacao que pegaria a divergencia.
- **[6d — nota de metodo] Numeracao de CASO colidiu entre blocos, e o script que anexa teste duplicou em silencio.** Auditoria do `webhook.integration.test.ts` encontrou os CASOS 20 a 27 DUPLICADOS: eu numerava contando a partir do ultimo numero que EU tinha usado, e nao do maior em uso no arquivo — os casos 20-25 do Bloco 6c ja estao na `main` com numero colidindo. Isso importa porque numero de caso e referencia em artefato duravel: este documento e as descricoes de PR citam "CASO 23", e cada citacao apontava para DOIS testes. Os onze casos que eu adicionei (6c e 6d) foram renumerados para 34-44, e as referencias deste documento corrigidas. Separadamente, quatro testes tinham sido adicionados DUAS vezes, porque meus scripts anexam sem verificar se o marcador ja existe — rodar o mesmo script duas vezes duplicava sem aviso, e eu reportei `719/110` como evidencia sem conferir que o numero batia com os casos que eu havia escrito (deveria ser 717/108). **Duas regras adotadas:** ler o maior numero em uso ANTES de numerar caso novo, e abortar o script se o marcador ja existir no arquivo.
- **[6d REABERTA — prevenida por guardas SEM teste de concorrencia; claim NAO implementado] Claim de posse na linha do inbox.** Esta entrada foi escrita no Bloco 4 afirmando que duas execucoes concorrentes podiam gravar conclusoes uma por cima da outra. Percorrido o codigo atual, isso deixou de ser verdade: entraram tres guardas de estado (`catch`, `encerrar` e o ramo retentavel, todas com `status in (RECEIVED, FAILED)`) e o 6c ainda as inverteu para fail-closed. Cenario a cenario — A conclui PROCESSED e B tenta depois: `updateMany` de B casa zero linhas; A conclui IGNORED e B falha depois: idem; ambas aplicam o efeito: o CAS do `Payment` deixa so uma passar, e `escreverTrilha` vive DENTRO da transacao vencedora, entao trilha duplicada e impossivel. Implementar lease com token, TTL e propagacao por todos os caminhos de escrita protegeria algo que as guardas ja protegem. RESIDUAL REAL, menor e nomeado: duas execucoes concorrentes que falham incrementam `attempts` duas vezes pela MESMA entrega, antecipando o teto do 6c — metrica, nao dinheiro. **Destino:** Fase 10, junto do claim atomico do relay, se e quando houver evidencia de entregas concorrentes reais. **Gatilho:** quarentena por teto acontecendo com menos reentregas que o configurado. **REABERTA apos o review (achados 4.4 e 5.1), com duas correcoes minhas:** primeiro, classificar o residual como "metrica, nao dinheiro" estava ERRADO — `attempts` participa do teto do 6c, entao a dupla contagem produz QUARENTENA PREMATURA, que e disponibilidade: um evento legitimo desiste com menos reentregas que o configurado. Segundo, dizer que o risco "nao existe mais" e forte demais: ele esta PREVENIDO por guardas que nao tem teste de concorrencia, o que e diferente. O CASO 44 cobre uma parte (duas entregas reais aplicando o efeito produzem UMA captura e UM evento de outbox); continuam sem teste: `PROCESSED` contra falha, `IGNORED` contra falha, e duas falhas incrementando `attempts`. **Destino:** Fase 10. **Gatilho para promover:** quarentena por teto acontecendo com menos reentregas que o configurado — que e o sintoma observavel do residual.


- **[6d — 5a rodada de review] O portao de plausibilidade estava tres passos acima do lugar certo e retinha DOIS caminhos que nao lhe pertenciam** (achados 4.1 e 4.2). O portao existe para proteger UMA coisa: a escrita de `lastProviderEventAt`. Posicionado antes do roteamento de reembolso e da classificacao de `unsupported`, ele retinha os dois. No reembolso o dano e financeiro e contradiz a decisao declarada NESTE documento e no schema — um `refund.succeeded` seis minutos a frente ficava retentavel, nunca aplicava o delta sobre `refundedAmountCents` e terminava em quarentena por idade: dinheiro devolvido pelo provedor sem registro nosso. No `unsupported`, um evento que deveria ser terminal virava 503 e o provedor reentregava para sempre um evento que nunca seria aplicado. Movido para DEPOIS dos dois. Cobertura nova: reembolso com timestamp futuro aplica o delta mesmo assim (CASO 46) e nao-suportado com timestamp futuro e terminal, nao 503 (CASO 47). Sabotagem H-1 (portao de volta ao lugar antigo) derruba os dois.
- **[6d — nota de metodo] Politica declarada em comentario nao tinha NADA que a ligasse ao codigo.** A excecao do reembolso a ordenacao por timestamp estava escrita em tres lugares — comentario do schema, comentario do servico e este documento — e mesmo assim eu a violei pela POSICAO de um portao, nao por discordar dela. Prosa nao e mecanismo: nenhum teste nomeava a politica, entao nada quebrou quando ela deixou de valer. Regra adotada: politica escrita em comentario precisa de um teste que a nomeie, ou nao esta protegida por nada. Os CASOS 45 e 46 sao esse teste para a excecao do reembolso.
- **[6d PAGO — sabotagem vazia virou teste, pela SEGUNDA vez nesta fase] A verificacao do `count` nos ramos retentaveis** (achado 4.3). Os dois ramos retentaveis (`providerRef ainda desconhecido` e `providerCreatedAt alem da tolerancia de futuro`) gravavam o motivo com `updateMany` guardado por `status in (RECEIVED, FAILED)` e IGNORAVAM o `count`. Consequencia: se uma execucao concorrente concluisse a linha entre a DECISAO e a ESCRITA, respondiamos 503 pedindo reentrega de um evento JA aplicado. Corrigido nos dois de uma vez, extraindo `comoRetentavel` — corrigir so o ramo novo criaria assimetria entre caminhos que fazem a mesma coisa. A sabotagem H-2 passou VAZIA na primeira bateria, e eu previ que passaria; nao virou divida porque a lacuna era de TESTE, nao de testabilidade: `WebhookService` ja recebe o cliente Prisma por `deps`, entao o CASO 48 embrulha o delegate `webhookEvent` num Proxy que conclui a linha entre o `updateMany` do `comoRetentavel` e a resposta, e afirma 200 em vez de 503. Sabotagem reaplicada: passou de vazia para derrubar o CASO 48. **Regra adotada:** antes de registrar uma sabotagem vazia como divida alegando impossibilidade de teste, verificar se o colaborador ja e injetavel — o A-8 do 6c foi o primeiro caso, este e o segundo, e nos dois a injecao ja existia em producao. `NADA FALHOU` e ausencia de teste ate prova em contrario, nao prova de inatingibilidade.
- **[6d — nota de metodo] Referencia cruzada por NUMERO de caso e fragil, e a nota sobre nao conferir aritmetica continha erro de aritmetica** (achados 5.1, 5.2 e 5.3). (a) Este documento citava casos por numero ("CASO 23"), e a renumeracao do proprio bloco invalidou as citacoes; as referencias passaram a ser DESCRITIVAS, com o numero como complemento e nao como identidade. (b) A nota que registrava que eu havia reportado `719/110` sem conferir a aritmetica trazia ela mesma uma soma errada, corrigida para 717/108 — errar aritmetica dentro da nota sobre errar aritmetica e o tipo de coisa que so aparece se alguem reconferir cada numero, e o revisor reconferiu. (c) A renumeracao foi aplicada FISICAMENTE no arquivo de teste, com mapa 43 -> 44, 44 -> 45, 45 -> 43, 90 -> 46, 91 -> 47, para nao existirem dois esquemas de numeracao convivendo.
- **[6e PAGO] Expiracao da janela: o ultimo caminho por onde um pagamento ficava preso.** Populacao COMPLEMENTAR a do 6b — `AUTHORIZE` PENDING **com** `providerRef`, o rastro do aceite assincrono: a cobranca existe e o provedor nunca concluiu. O 6b nao alcanca (a populacao dele e `providerRef` nulo) e o 6c nao ajuda, porque nenhum webhook chega. `EXPIRED` existia no enum desde o Bloco 3 e NUNCA tivera produtor; este bloco e o primeiro. A varredura COMANDA `cancelCharge` e aplica o que voltar: `CANCELED` expira, `SUCCEEDED` aplica a CAPTURA, `DECLINED` aplica a falha, `PROCESSING` vai a triagem (nao ha `aguardar` — ja comandamos). Falha tecnica LANCA e a tentativa continua presa e visivel, mesma assimetria do 6b. Componentes: `jobs/expiracao.ts` (decisao pura + tick + varredura), `expiracao.repository.ts`, `expiracao.deps.ts`, `PaymentService.expirarTentativa` e `aplicarDesfechoDeExpiracao`, terceira entrada no `startJobs`. Duas extracoes no SEGUNDO uso, ambas pelo criterio de que o COMPILADOR nao pega a divergencia: `jobs/keyset.ts` (paginacao por chave, cuja copia divergente produziria starvation silencioso) e `reivindicarEAplicar` (a condicao do CAS vive num WHERE). A traducao `ChargeSnapshot -> ChargeResult` NAO foi extraida, pelo criterio inverso: os campos vem de `ChargeResult` e `tsc` derruba os dois lugares. Prova (2026-09-02): `npm run verify` 728/728 mais build; `npm run verify:integration` 123/123. CASOS E1-E11 (integracao), X1-X5 (decisao pura) e Y1-Y5 (varredura e fiacao). Sete sabotagens: seis derrubaram o alvo, uma passou vazia e virou teste no proprio bloco (item abaixo).
- **[6e PAGO] Erro TIPADO para cobranca que nao admite cancelamento.** `cancelCharge` recusa a cobranca ja capturada, e a unica forma de distinguir essa recusa de "requisicao malformada" era comparar a MENSAGEM — no ramo em que errar significa tratar como falha tecnica um pagamento efetivamente COBRADO do cliente, deixando-o preso para sempre. A porta ja proibia isso na propria secao de erros ("`retryable` e dado no erro, e nao inferido de string"); faltava a classe. `ChargeNotCancelableError` estende `PaymentProviderError` DIRETO e nao `ProviderInvalidRequestError`: a requisicao esta correta, quem nao permite e o ESTADO. Mudanca de CONTRATO deliberada — a suite que rodara contra a Stripe no Bloco 9 passa a exigir a classe nova, e o adapter tera de mapear. CASOS Y2 e E10; sabotagem S3 derruba.
- **[6e PAGO — sabotagem vazia virou teste, TERCEIRA vez nesta fase] `assertTransicao` no `expirarTentativa`.** A sondagem S7 passou vazia: remover a guarda nao derrubava nada, porque nenhum teste montava um pagamento em estado do qual expirar e proibido. Era testavel e trivialmente: pagamento CAPTURED com a tentativa ainda PENDING. O CASO E11 prova DUAS coisas de uma vez, porque o CAS roda ANTES da guarda — a recusa, e o ROLLBACK da marcacao ja feita. Sem a atomicidade, a tentativa ficaria FAILED com `EXPIRADO_JANELA` sobre um pagamento capturado. **A regra, agora com tres ocorrencias (A-8 no 6c, H-2 no 6d, S7 aqui):** `NADA FALHOU` e ausencia de teste ate prova em contrario. Antes de registrar como divida, tentar escrever o teste.
- **[6e DEVOLVIDO — destino: BLOCO 6f, os dois servicos no MESMO PR] A expiracao NAO compensa a saga, e publicar sem o consumidor QUEBRA.** O comentario do enum diz "EXPIRED — janela esgotou sem captura (Bloco 6 compensa a saga)", e essa promessa continua nao cumprida: a outbox do payment publica UMA routing key, `payment.captured`, e o pedido do lado do order-service fica pendurado quando o pagamento expira. Verificado, nao suposto: `payments.topology.ts` do order-service usa binding ESTRITO (`payment.captured`), com o comentario registrando que binding largo deixaria evento futuro chegar sem tratamento. Como o publisher do payment usa `mandatory` + `basic.return`, publicar `payment.expired` hoje produziria mensagem NAO ROTEAVEL — falha alta, nao silenciosa. Por isso o 6e nao publica nada: o evento e o binding tem de entrar juntos. **O 6e nao piora nada** — hoje o pedido ja fica pendurado, com o pagamento preso em PROCESSING; depois dele o pagamento fica EXPIRED, terminal e visivel, e a cobranca morta no provedor (dinheiro liberado no cartao, que e o dano concreto). **Essencial antes de fechar a Fase 5.**
- **[6e — pode esperar | destino: BLOCO 9, junto do adapter da Stripe] A janela de expiracao e a mesma da reconciliacao, e com provedor real ela precisa ser POR METODO.** `paymentWindowMinutes` foi reusado em vez de criar `PAYMENT_EXPIRATION_MINUTES`: nao ha evidencia de que os valores precisem divergir, e a cadeia do invariante temporal ja tem tres termos. O RISCO, nomeado: cancelar cedo demais mata uma cobranca que o cliente ainda esta completando. Com cartao em 3DS, boleto ou PIX, o tempo legitimo de conclusao varia por METODO, e uma janela unica ou cancela cedo os lentos ou demora demais nos rapidos. **Gatilho:** primeiro metodo de pagamento assincrono lento entrar no servico.
- **[6e — nota de metodo] Introduzi uma ferramenta que o projeto NAO usa, e ela reformatou arquivos inteiros.** Rodei `npx prettier --write` por habito, sem verificar: nao ha `.prettierrc`, nao ha `.editorconfig`, nao ha script de format no `package.json`. O padrao do prettier (aspas duplas) reescreveu os arquivos inteiros — `fake.provider.ts` saiu com **298 linhas** de diff para uma mudanca de 2, e `payment-provider.contract.ts` com **329** para 2. As suites ficaram VERDES o tempo todo, entao nada avisou; o que expos foi Davi colar um output com aspas duplas. Corrigido restaurando os arquivos pre-existentes da `main` e reaplicando so a mudanca real. **Regra:** nao introduzir ferramenta que o projeto nao usa; antes de formatar, verificar como o projeto formata.
- **[6e — nota de metodo] `.strip()` numa ancora de linha unica joga fora a indentacao, que e o discriminante.** Depois do 6d eu adotei "ancora de linha unica" como regra segura contra edicao errada, e apliquei com `l.strip() == ...`. O alvo `await tx.idempotencyRecord.update({` existe DUAS vezes no `payment.service.ts`, em indentacoes diferentes; o `strip` casou a errada, e o splice seguinte teria substituido **117 linhas**, apagando o `aplicarDesfechoDeReconciliacao` inteiro. So nao aconteceu porque o script morreu antes de gravar — sorte, nao metodo. Separadamente, um `str.replace` que nao casou falhou em SILENCIO (replace devolve a string intacta), e so o `tsc` denunciou. **Duas regras:** ancora de linha unica exige comparacao EXATA, com indentacao; e toda substituicao tem de verificar que aplicou, com abort explicito.
- **[6a DEVOLVIDO] CHECK `idempotency_completed_exige_resposta` em migration POSTERIOR (expand/contract).** Ele existiu no primeiro commit do PR #56 e foi RETIRADO no review (achado 4.1): mesmo `NOT VALID`, um CHECK passa a valer para todo `UPDATE`, e uma instancia da versao ANTERIOR ainda em execucao conclui a chave sem preencher a coluna — o `UPDATE` seria recusado DEPOIS de o provedor ja ter cobrado, deixando o pagamento preso em `PROCESSING`. Sequencia correta: (1) coluna nullable [feito]; (2) todas as instancias escrevendo e lendo o snapshot; (3) confirmar que nao ha escritor antigo; (4) migration com o CHECK `NOT VALID`; (5) backfill ou expiracao; (6) `VALIDATE CONSTRAINT`. Ate o passo 4, o invariante e mantido apenas pela aplicacao.
- **[6a DEVOLVIDO] Registros legados sao PRE-CONDICAO operacional, nao detalhe.** Chave `COMPLETED` anterior a esta entrega nao tem resposta congelada e continua sujeita ao defeito que o bloco corrige: pode responder `CAPTURED` depois que outra chave capturar o mesmo pedido. Antes de considerar a correcao completa, inventariar essas linhas.
- **[6a DEVOLVIDO] O backfill NAO pode usar o `Payment` vivo como fonte unica.** Registrado aqui porque a versao anterior deste documento recomendava exatamente isso — e o `Payment` vivo pode conter o desfecho de OUTRA tentativa, que e a causa do defeito. Preencher assim transformaria uma resposta temporariamente errada em snapshot permanentemente errado, e depois validaria a constraint em cima dele. Sem vinculo confiavel entre chave e tentativa historica, EXPIRAR as chaves antigas conforme politica e mais seguro que reconstruir o resultado original. Achado 4.3 do review do PR #56.
- **[6a] Ramo LEGADO do replay.** Enquanto houver `COMPLETED` sem `completedResponse`, o replay reconstroi a resposta a partir do `Payment` vivo e LOGA — com o defeito antigo (pode devolver o desfecho de outra tentativa no mesmo pedido). E declarado, nao silencioso, e some junto com o `VALIDATE CONSTRAINT`. Nao apagar este ramo antes de validar a restricao.
- **[6a] Retencao de `completedResponse`.** A coluna e pequena e nao guarda nada do provedor alem do `declineCode`, entao nao cria exposicao nova — mas cresce com a tabela, que ja nao tem politica. Tratar junto com a retencao de `idempotency_records`, nunca isolado.
- **[6b PAGO] Job de reconciliação de tentativas presas.** Varredura periódica que pergunta ao provedor o desfecho de cada `AUTHORIZE` que ficou `PENDING` sem `providerRef` por mais que a janela, e aplica pelo MESMO caminho do fluxo normal (`aplicarDesfecho`) ou libera a tentativa quando o provedor prova que nada chegou. Componentes: `src/jobs/reconciliacao.ts` (decisão pura + varredura), `reconciliacao.repository.ts` (consulta de candidatos), `reconciliacao.deps.ts` (montagem da fiação), `reconciliacao.runtime.ts` (ciclo de vida, poll default 60_000ms) e duas operações em `PaymentService` com compare-and-swap na MESMA transação do desfecho.
  DECISAO: a porta ganhou `buscarCobrancaPorTentativa(paymentId, attemptCount)`. Falha tecnica LANCA; a tentativa continua presa. Assimetria deliberada: aplicar desfecho errado mexe em dinheiro e e irreversivel do ponto de vista do cliente; nao aplicar mantem o estado atual, que ja e visivel.
  O fake ganhou o token `TIMEOUT_AFTER_CHARGE`, que CRIA a cobranca e so entao falha. Sem ele nao havia como montar o estado preso de ponta a ponta: o fake antigo nunca criava nada no erro, e todo teste do job seria teatro sobre linhas escritas a mao.
  Prova (2026-08-29, commit 63d6672): `npm run verify` 669/669 em 25 suites, mais build; `npm run verify:integration` 95/95 em 4 suites. Onze sabotagens em duas rodadas, com a arvore commitada e cada probe rodando na suite onde o teste do alvo vive. NOVE derrubaram o alvo; DUAS passaram vazias e viraram teste no proprio bloco (itens abaixo).
- **[6b PAGO] Progresso garantido na varredura** (achado 4.1 do review do PR #57). `aguardar`, `triagem` e falha NAO alteram a linha: ela continua `PENDING` sem `providerRef` e continua entre as mais antigas. Com `orderBy createdAt asc` + `take`, um lote de itens nao-acionaveis congelava a varredura para sempre e nenhuma tentativa posterior era examinada — bastava o provedor cair durante um ciclo. Corrigido com paginacao por CHAVE (`(createdAt, id) > cursor`), nao por `skip` nem pelo `cursor` nativo do Prisma: offset se desloca quando uma linha e resolvida no meio da varredura, e o cursor nativo localiza a linha por id, ficando indefinido se ela deixou de ser candidata. Sem migracao. CORRECAO da 2a rodada de review: a primeira entrega paginava apenas DENTRO do ciclo — o cursor era local ao tick e voltava a `undefined` no ciclo seguinte, entao com `maxLotes * lote` itens nao-acionaveis cada ciclo relia os mesmos e o item seguinte nunca era alcancado. O starvation tinha sido deslocado de 20 para 100 registros, nao removido, e a frase "progresso garantido" era superestimada. Agora o cursor sobrevive entre ciclos (`criarVarredura`, estado em fechamento e nao de modulo) e faz wrap-around quando a fila esgota, senao os mais antigos deixariam de ser reavaliados. Prova: CASOS S8 (item alem do primeiro lote, cursor tirado da ULTIMA linha de uma pagina de DOIS itens — com pagina de um, "ultimo" e "unico" coincidem e a assercao nao distinguiria), S9 (teto marca `truncada`), S11 (dois ciclos consecutivos alcancam o terceiro item; sem memoria a sequencia seria a, b, a, b), J8 (keyset REAL contra Postgres, com desempate por `id` entre linhas de mesmo `createdAt`) e J9 (linha do cursor que deixou de ser candidata nao perde a pagina seguinte — a razao de nao usar o `cursor` nativo do Prisma, que localiza a linha por id). As cinco sabotagens correspondentes derrubam exatamente esses casos.
- **[6b PAGO — precondicao imposta pelo codigo] Ausencia definitiva e DECLARADA pela porta** (achado 3.1). A regra `null -> liberar` supunha consulta read-after-write consistente no provedor, suposicao que nunca foi exercitada porque o fake responde da propria memoria. Com um provedor eventualmente consistente, um `null` FALSO libera para nova cobranca uma tentativa que ja cobrou — SEGUNDA COBRANCA, exatamente o que o job existe para evitar. Correcao no modelo da flag `PAYMENTS_CONSUMER_ENABLED` do 5b: `PaymentProvider.ausenciaEDefinitiva` e obrigatorio, e `decidirReconciliacao` exige o valor como parametro SEM default — escolher em silencio era o risco. Com `false`, a ausencia vira TRIAGEM e o pagamento continua preso e visivel, que e o estado seguro. O compilador impede qualquer adapter novo (a Stripe do Bloco 9) de herdar o silencio.
  CRITERIO CORRIGIDO na 2a rodada de review: consistencia read-after-write NAO basta e a primeira versao deste comentario dizia que bastava. Ela garante visibilidade depois que uma escrita TERMINOU, e nada diz sobre uma cobranca que ainda VAI ser criada — requisicao em voo, ou aceita pelo provedor e ainda nao materializada; nesse caso o `null` e verdadeiro na leitura e falso um segundo depois. So pode declarar `true` o adapter cujo provedor garanta que "nao encontrado" e TERMINAL para aquela correlacao: nenhuma operacao com ela pode mais ser criada nem concluida. A janela de retentativa e condicao NECESSARIA, nunca suficiente.
  NAO foi adicionado teste de contrato para isso, deliberadamente: um teste create-e-consulta contra fake em memoria passa sempre, e nao pode falhar pelo motivo que importa — um adapter que declare `true` sem a garantia real. Seria mecanismo sem prova disfarcado de cobertura. A forca aqui e o tipo, nao o teste. Prova do que E testavel: CASO R7 (decisao pura) e CASO S10 (a varredura repassa a declaracao em vez de fixar `true`).
- **[6b DESCOBERTO — corrigido dentro do bloco] O CAS de `liberarTentativaPresa` nao tinha teste que o pegasse.** O CASO J2 exercita o metodo sobre tentativa ainda `PENDING`, onde o `where` nunca precisa filtrar nada: remover `status: PENDING` e `providerRef: null` deixava as DUAS suites verdes. Sem a condicao, um `liberar` decidido sobre snapshot obsoleto sobrescreve um desfecho JA aplicado — transacao e `Payment` viram `FAILED` com o provedor tendo cobrado, e o `providerRef` some da trilha. Coberto pelo CASO J7, com a sabotagem reaplicada para confirmar que ele a mata.
- **[6b DESCOBERTO — corrigido dentro do bloco] A fiacao das deps do job nao tinha teste.** Enquanto a montagem vivia dentro do `server.ts`, trocar `provider.ausenciaEDefinitiva` por `true` desligava a protecao do achado 3.1 inteira sem derrubar um teste sequer: o `bootstrap.test.ts` injeta as deps e nunca alcanca aquela fiacao. Extraida para `montarDepsDeReconciliacao` (funcao pura, sem mock de modulo) e coberta por D1-D4; a sabotagem passou de vazia para derrubar o D1.
- **[6b DEVOLVIDO — destino: FASE 10, secao Jobs/reconciliacao] Triagem sem estado duravel, e teto residual da varredura.** `triagem` incrementa contador e loga; ninguem e dono, ninguem e alertado, e o item volta a cada ciclo. Com o cursor sobrevivendo entre ciclos, o bloqueio dos posteriores acabou: todo candidato e alcancado em, no maximo, `ceil(N / (maxLotes * lote))` ciclos. O residual mudou de natureza e e de CUSTO, nao de disponibilidade — item permanentemente nao-acionavel volta a ser consultado no provedor a cada varredura completa, e quanto maior a fila presa, mais devagar os recentes sao revisitados. Correcao definitiva: status de triagem persistido, `nextReconcileAt`/backoff e retirada do item da varredura normal ate intervencao. Exige migracao — por isso nao entra numa fase que ja tem migracao em expand/contract pendente. **Gatilho para promover a essencial:** primeiro caso real de item em triagem, ou volume de presas acima de um lote.
- **[6b DEVOLVIDO — destino: FASE 10, secao Contratos/arquitetura] Sobreposicao de ciclos no runtime do job.** `stopReconciliacao` pode atingir `STOP_TIMEOUT_MS` e marcar `iniciado = false` com o ciclo antigo ainda em execucao; um `startReconciliacao` seguinte cria um segundo laco, e o ciclo antigo pode reagendar o proprio timer. Consequencia: consultas duplicadas ao provedor e mais disputa de CAS — nao ha risco financeiro, porque o CAS decide. Mesma classe do estado global de modulo do publisher e do relay, ja registrada nessa secao; a correcao e a mesma (geracao/token de execucao). Hoje `start`/`stop` acontecem uma vez por processo. **Gatilho:** reinicio do job sem encerrar o processo.
- **[6b DEVOLVIDO — destino: BLOCO 8, junto do item do relay] Ciclo de vida do job sem teste.** Sem cobertura: `startReconciliacao` idempotente, parada durante ciclo em voo, nao reagendar apos parada, e o teto do `stopReconciliacao`. Identico ao que ja esta registrado para `startOutboxRelay`/`stopOutboxRelay` — tratar os dois juntos, com o mesmo arcabouço.
- **[6b DEVOLVIDO — destino: BLOCO 10, junto do item do `providerRef`] O vinculo tentativa <-> chave de idempotencia e DERIVADO, nao armazenado.** Levantado no achado 4.2 do review. Verificado no codigo: o `update` que grava `paymentId` na chave vive DENTRO da transacao que cria o `Payment`, e o `encerrarClaimPreEfeito` deleta a claim perdedora quando o erro e retentavel; somado a matriz de estados (pagamento em `PROCESSING` nao admite tentativa nova), "no maximo uma chave `PROCESSING` por pagamento" e invariante REAL — e por isso `findFirst(..., createdAt desc)` e o `attemptCount` do `Payment` vivo estao corretos. Mas e invariante de APLICACAO, nao do banco, e tem um vazamento conhecido: `marcarChaveFalhada` engole a falha do `update` e apenas loga, deixando chave `PROCESSING` orfa. Mesma familia do item do `providerRef` sem unicidade. Correcao: `attemptCount` (ou `idempotencyRecordId`) na propria `PaymentTransaction`, incluido no CAS — exige migracao. **Gatilho:** qualquer mudanca na matriz de estados que permita tentativa nova com pagamento fora de `FAILED`. NAO ha teste fixando esta invariante — verificado por grep na suite de integracao: o caso mais proximo (`payment-flow.integration.test.ts:190`) prova o ESTADO preso, nao que `PROCESSING` recusa tentativa nova. Enquanto nao houver, a correcao do job depende de uma invariante que nenhum teste segura.
- **[6b — pode esperar | destino: FASE 7, junto do claim atomico do relay] O job nao tem lock entre instancias.** Com varias replicas, todas varrem o mesmo lote e consultam o provedor em duplicidade. CORRECAO de uma afirmacao anterior deste documento, apontada no review: dizer que o custo e "apenas chamada duplicada, nunca dinheiro duplicado" so vale SE a consulta ao provedor for consistente — duas instancias podem observar `null` e `SUCCEEDED` em instantes diferentes, e o CAS garante que uma vence, nao que a vencedora tinha a informacao certa. Com `ausenciaEDefinitiva = false` o caminho perigoso nem existe (vira triagem); com `true`, a garantia do provedor e o que sustenta a afirmacao. O custo em si continua sendo chamada duplicada.

- **[6c PAGO] O inbox de webhook para de girar, e quem desistiu fica visivel.** O diagnostico que abriu o bloco corrigiu o que este documento dizia: existem DUAS populacoes de reentrega, nao uma. (A) `decidirEAplicar` LANCA -> linha vira `FAILED`, `attempts` incrementa, rota responde 5xx, provedor reentrega. (B) desfecho `retentavel` (providerRef ainda desconhecido) -> linha continua `RECEIVED`, `attempts` NAO incrementa (achado 4.5 do Bloco 4, deliberado), rota responde 503, provedor reentrega. Um teto por contagem so alcanca A; B nao tinha limite nenhum.
  ESTADO NOVO `QUARANTINED` (migration aditiva `ALTER TYPE ... ADD VALUE`, sem tocar em linha existente). `IGNORED` e uma DECISAO ("nao se aplica a nos"); quarentena e uma DESISTENCIA. Misturar os dois destruiria a triagem, que e a unica razao de o estado existir.
  (A) Teto por `attempts`, com a condicao no WHERE (`attempts: { gte: teto }`) em vez de releitura: o banco avalia o valor JA incrementado na mesma instrucao que transiciona, e o `count` diz se houve transicao. Ao atingir o teto o erro NAO e relancado, para a rota responder 200 e o provedor PARAR.
  (B) Limite por IDADE do `receivedAt`, no mesmo idioma. Para essa populacao a pergunta e "ha quanto tempo esta inaplicavel", nao "quantas vezes falhou".
  O DETALHE QUE DECIDIU O BLOCO: `registrar` trata `QUARANTINED` como duplicata, junto de `PROCESSED` e `IGNORED`. As guardas do `catch` e do `encerrar` filtram `status in (RECEIVED, FAILED)` — se uma reentrega reprocessasse uma linha em quarentena, o efeito financeiro seria aplicado e o desfecho NAO conseguiria ser gravado: a linha continuaria `QUARANTINED` e a reentrega seguinte aplicaria DE NOVO. Captura dupla, silenciosa. Sair da quarentena e acao humana.
  VARREDURA do orfao, para o caso em que NENHUMA requisicao chega (provedor desistiu, ou o processo caiu entre gravar e aplicar): marca, e NAO reprocessa — reconstruir o `WebhookEventPayload` a partir do `payload` sanitizado por allowlist seria infiel, e aplicar dinheiro a partir de dado empobrecido e pior que deixar para triagem. `lastError` nao e sobrescrito: nas linhas `FAILED` ele guarda a causa real. Sem cursor nem teto de lotes, ao contrario do 6b — aqui a linha tratada SAI do conjunto, entao lote limitado progride por construcao.
  RUNTIME generalizado para uma lista de varreduras nomeadas (`startJobs`), com UM timer e `catch` POR varredura. A alternativa era um segundo runtime, duplicando start, stop com teto e guarda de parada — codigo que ja e divida por nao ter teste de ciclo de vida. Duplicar codigo nao testado e pior que generaliza-lo.
  CONFIG: `WEBHOOK_MAX_ATTEMPTS` e `WEBHOOK_QUARANTINE_MINUTES` entraram no `AppConfig`, e nao em `process.env` solto como os knobs do relay, pelo gatilho ja registrado ("reavaliar se algum passar a ter efeito operacional relevante") — decidem quando o servico PARA DE TENTAR aplicar evento financeiro. Ha invariante ENTRE campos validado no boot: a quarentena tem de ser MAIOR que `PAYMENT_WINDOW_MINUTES`, porque quem destrava a populacao B e o job do 6b, que so age depois da janela. O parser da quarentena tem teto proprio (7 dias): com o mesmo teto de 1440 dos dois, uma janela maxima tornaria o boot IMPOSSIVEL.
  Prova (2026-08-30, commit ad2181b): `npm run verify` 688/688 em 27 suites, mais build; `npm run verify:integration` 100/100 em 4 suites. Bateria de 12 sabotagens com a arvore commitada, cada probe na suite onde vive o teste do alvo: 11 derrubaram o alvo, 1 passou vazia (item abaixo), e a bateria terminou com as duas suites verdes sobre a arvore restaurada.
- **[6c PAGO — era divida, virou teste] A reavaliacao de estado na escrita da varredura.** A sabotagem A-8 da 1a bateria passou VAZIA: remover `status in (RECEIVED, FAILED)` do `updateMany` de `quarentenarOrfaos` nao derrubava nada. Eu havia registrado como divida alegando que prova-la exigiria gancho de teste dentro do codigo de producao — ERRADO, e apontado no review: o resto do projeto INJETA o cliente Prisma (`PaymentService`, `WebhookService`), e a funcao usava `getPrisma()` direto. Passou a receber o cliente por parametro (com o global como default), o que e injecao e nao gancho, e o CASO 37 entrelaca uma conclusao REAL entre o `findMany` e o `updateMany`. Sabotagem reaplicada: passou de vazia para derrubar o CASO 37. Dano que a guarda evita: `PROCESSED` sobrescrito por `QUARANTINED` — trilha mentindo que desistimos de um evento que foi aplicado.
- **[6c DEVOLVIDO — PROCEDIMENTO de deploy, nao trabalho pendente] Ordem de implantacao do estado `QUARANTINED`.** `ALTER TYPE ... ADD VALUE` e aditivo e nao toca em linha existente — diferente do `CHECK` retirado no 6a, que passava a valer para todo `UPDATE`. O risco aqui e outro: uma instancia da versao ANTERIOR que leia uma linha `QUARANTINED`. Nao foi possivel verificar se o client Prisma antigo materializa um valor de enum que ele nao conhece, e por isso a correcao nao foi supor comportamento, e sim tornar o codigo fail-closed: `registrar` passou a listar os estados ABERTOS (`RECEIVED`, `FAILED`) em vez dos terminais, entao qualquer valor desconhecido e tratado como TERMINAL. Sequencia correta quando houver frota: (1) implantar a versao com a lista invertida; (2) confirmar que nao ha instancia antiga; (3) aplicar a migration; (4) habilitar escritores de `QUARANTINED`. HOJE nao ha coexistencia possivel: o payment-service ainda nao esta no docker-compose (divida registrada na Fase 7), entao nao existe frota. Isto e precondicao para quando existir. **DEMONSTRADO, nao suposto (3a rodada):** `grep -n payment docker-compose.yml` nao retorna ocorrencia nenhuma e `services/payment-service/Dockerfile` nao existe. O servico nao esta implantado, entao nao ha frota e nao ha coexistencia de versoes possivel hoje. Por isso NAO foi adicionada flag de gate para os escritores de QUARANTINED: seria uma flag permanentemente ligada, que ninguem jamais desligaria, para um cenario que ainda nao existe. Quando o servico entrar no compose (divida da Fase 7), a sequencia acima passa a ser precondicao de implantacao.
- **[6c — 2a rodada de review] Correcoes que a rodada produziu, todas com teste.** (a) Invariante de config estava INCOMPLETO: eu exigia quarentena > janela argumentando que a reconciliacao destrava o evento, mas o intervalo do job era lido de `process.env` e nao participava de validacao nenhuma — janela 1 min, quarentena 2 min e poll 60 min passavam no boot e quarentenavam antes de o job ter qualquer chance. Os tres knobs do runtime entraram no `AppConfig` e o invariante virou `quarentena > janela + poll` (arredondado para cima). (b) Uma varredura PENDURADA travava todas: o `catch` so pega rejeicao, e uma promessa que nunca resolve segurava o `await` do ciclo e impedia o proximo timer — prazo por varredura (CASO N6). (c) A transicao para quarentena nunca passara pela ROTA; os CASOS 38 e 39 provam o mapeamento `retentavel === true ? 503 : 200` nas duas direcoes contra HTTP real. (d) O contrato de boot chamava-se `iniciarReconciliacao` depois de passar a controlar duas varreduras — renomeado para `iniciarJobs`/`pararJobs`. (e) `WEBHOOK_MAX_ATTEMPTS` e `WEBHOOK_QUARANTINE_MINUTES` entraram no `.env.example` com faixa, default e o invariante escrito.
- **[6c — 3a rodada de review] A correcao do prazo tinha criado um vazamento; corrigida com single-flight.** O prazo por varredura (2a rodada) devolvia o controle ao laco mas ABANDONAVA a operacao: a original seguia viva, o ciclo seguinte iniciava OUTRA execucao da mesma varredura, e elas se acumulavam — conexoes, chamadas ao provedor e concorrencia sobre o mesmo trabalho. Pior, ao expirar a promessa saia do `cicloAtual` e o `stopJobs` encerrava banco e publisher POR CIMA dela. Trocar travamento por vazamento nao e conserto. Agora: single-flight por varredura (enquanto o trabalho nao assenta, a varredura e pulada nos ciclos seguintes, com log), rastreamento das promessas expiradas para o shutdown espera-las dentro do teto, e tratador para rejeicao tardia — que sem isso viraria unhandled rejection e sumiria do log, justamente o caso que mais precisa aparecer. CASOS N7 (nao reinicia), N8 (volta a rodar quando assenta), N9 (rejeicao tardia registrada) e N10 (continua rastreada). NAO foi implementado cancelamento real: `AbortSignal` propagado ate o driver do banco e o cliente do provedor e reescrita da camada de dados, e fica registrado como o passo seguinte se o prazo passar a estourar de verdade.
  O INVARIANTE temporal tambem estava otimista pela segunda vez: somava o intervalo do poll, mas o proximo ciclo so e agendado depois que TODAS as varreduras terminam, e cada uma pode consumir o prazo inteiro. Passou a ser `quarentena > janela + poll + (prazo x VARREDURAS_POR_CICLO)`. A constante vive no `env.ts` e o `server.ts` verifica que a lista real tem esse tamanho — constante que diverge do codigo em silencio e pior que constante nenhuma.
  Prova (2026-08-31): `npm run verify` 711/711; `npm run verify:integration` 103/103.
  Prova (2026-08-30): `npm run verify` 706/706; `npm run verify:integration` 103/103. Duas sabotagens reaplicadas apos as correcoes (B-1: `QUARANTINED` volta a ser tratado como aberto; B-2: varredura sem reavaliacao de estado) derrubaram os casos 20 e 23 respectivamente.

### Bloco 8 — Bateria de testes
- **Provar ROLLBACK de escrita parcial no `$transaction` de `persistirTentativa`.** O que já está provado: o duble de Prisma cobre ordem das operações e decisões, e a integração cobre os `CHECK`, o `@unique` de `orderId` e concorrência real. O que **não** está provado: que uma falha *depois* de uma escrita bem-sucedida dentro da mesma transação desfaz a anterior. No teste de concorrência a falha acontece no primeiro `create`, então não há escrita prévia para desfazer. Exige injetar falha no meio da transação — por exemplo forçar violação de `CHECK` no `paymentTransaction.create` após o `payment.update` de `attemptCount`, e conferir que o contador volta ao valor anterior.
- **Cobrir o `getPrisma` sem `connectDatabase` prévio.** O caminho de erro existe e tem mensagem explícita, mas nenhum teste o exercita.
- **A publicacao NUNCA foi exercitada contra um RabbitMQ real.** No Bloco 5a o `publish` e sempre duble: os testes provam a DECISAO do relay (marcar SENT so apos confirm, nao tocar nos eventos com broker fora, nao reentrar) e a gravacao na outbox, mas nenhum byte chegou a um broker. Conexao, `assertExchange`, confirm channel e reconexao sao codigo copiado do order-service e nao verificado aqui.
- **`startOutboxRelay`/`stopOutboxRelay` parcialmente cobertos.** O ciclo de vida os CHAMA na ordem certa (sabotagens W1-W4), o CASO A9 prova que o start agenda o proximo ciclo e o A10 que o stop impede novos ciclos. CONTINUA sem teste: o teto do `stopOutboxRelay` (`STOP_TIMEOUT_MS`), o destino de um tick ainda em voo quando esse teto estoura, e a idempotencia de dois `start` seguidos.


### Bloco 9 — Stripe e hardening
- **Rodar a suíte de contrato (`payment-provider.contract.ts`) contra a Stripe.** É o que valida a abstração da porta.
- **Sanitização de log**, rate limit no webhook, escopo PCI documentado.
- **Webhook confia no payload assinado em vez de re-buscar via `getCharge`.** O handler do Bloco 4 usa `state` e `capturedAmountCents` do corpo verificado por HMAC. A assinatura prova autenticidade dos bytes, entao o valor e autentico enquanto o segredo estiver integro, e a obsolescencia ja e coberta pela maquina de estados. Com provedor real, re-buscar o snapshot antes de aplicar efeito financeiro e o hardening esperado. A coerencia de valor contra `Payment.amountCents` ja e verificada hoje.
- **Nenhum log em 401/400 na rota de webhook.** Assinatura forjada e evento invalido sao recusados em silencio: zero observabilidade de tentativa de forja. Entra junto do rate limit ja registrado nesta secao, com log sanitizado (nunca o corpo, nunca o cabecalho de assinatura). Levantado no review do PR do Bloco 4.
- **Revisitar o REQUISITO de gravar `raw` no inbox.** Cinco rodadas de review encontraram defeito na sanitizacao do payload bruto: aliases PCI, estrutura aninhada, colisao de normalizacao, heranca de prototipo. O filtro esta correto hoje e provado por sabotagem, mas a alternativa estrutural e nao gravar `raw` e sim o `WebhookEventPayload` ja validado — sem dado desconhecido, nao ha o que filtrar. Nao foi feito no Bloco 4 porque contraria decisao explicita do Bloco 3 (o `fake.wire` guarda `bruto` para preservar campo extra, e o schema documenta o payload como evidencia para reprocessamento). Se aparecer uma sexta rodada de defeito nesse filtro, a correcao certa e revisitar o requisito, nao o filtro.


### Bloco 10 — Fechamento
- **Finalizar o README do payment-service e revisar o README da raiz.** Ambos foram *criados/marcados* no PR de manutenção pré-Bloco 3, com o estado daquele momento. O que falta e a revisão final: descrever os endpoints, o fluxo completo e trocar o marcador 🟡 por ✅ quando a fase fechar.
- **PDF de revisão da fase** em `docs/phase-reviews/phase-05.pdf`.
- **Avaliar ligar `noUnusedLocals`/`noUnusedParameters` no payment-service.** Hoje o tsconfig tem apenas `strict: true`, e um import sem uso compila sem aviso — foi assim que `decidirReconciliacao` ficou importado no `server.ts` depois de a fiacao sair de la (achado 5.1 da 2a rodada de review do PR #57, ja removido). Mesma natureza do item do `lib` acima: mudanca de configuracao que afeta o servico inteiro e pode revelar outros pontos, entao nao entra em PR de feature.
- **Avaliar subir `lib` para `es2022` no tsconfig do payment-service.** Hoje `Array.prototype.at` não compila, e o teste teve de usar aritmética de índice. O Node 22 suporta. Mudança de configuração que afeta todo o serviço e pode revelar outros erros — não entra em PR de feature para não misturar escopo.
- **`WebhookEvent.lastError` guarda tambem o MOTIVO de `IGNORED`,** que nao e erro. O nome da coluna e mais estreito que o uso real. Renomear para `lastReason` custa migracao; entra junto da limpeza das dividas pagas, para nao gastar migracao isolada no meio da fase. O Bloco 6c ampliou o MESMO desvio para `processedAt`, que passa a marcar tambem o instante em que DESISTIMOS de um evento (quarentena): o `WebhookEvent` nao tem `updatedAt`, entao sem isso esse momento se perderia. Renomear os dois na mesma migracao.
- **`PaymentTransaction.providerRef` nao tem constraint de unicidade.** O schema tem `@@index([providerRef])`, que e indice e nao restricao. O handler do webhook busca filtrando `type: AUTHORIZE` e ordenando por `createdAt asc`, entao o resultado e DETERMINISTICO mesmo com duplicata. Na pratica nao ha duplicata porque a chave de idempotencia do provedor deriva de `paymentId:attemptCount` e cada tentativa gera cobranca nova — mas isso e invariante de APLICACAO, nao do banco. Avaliar `@@unique` parcial ou documentar a garantia. Levantado na 6a rodada de review do PR do Bloco 4.



### Gatilho - captura em duas fases (fora da Fase 5)
- **Linha `VOID` quando `CANCELED` chega sobre autorizacao ja SUCEDIDA.** O handler do webhook fecha a `AUTHORIZE` em `PENDING` como `FAILED` + `PROVIDER_CANCELED`. O ramo em que a autorizacao ja sucedeu exigiria uma linha `VOID`, e ele nao existe hoje porque `AUTHORIZED` nao tem produtor: `mapearEstadoDoProvedor` nao mapeia nenhum `ChargeState` para ele, por decisao de captura automatica (decisao 10 da fase). Implementar agora seria codigo morto. Gatilho: quando a captura em duas fases entrar, no fluxo de expedicao.


## FASE 7 — Gateway, Segurança e Infra

### Segurança
- **JWT hardening — `issuer`/`audience` e shape do payload.** O que resta aqui e apenas isto: `jwt.verify` com `issuer` e `audience` explicitos, e validacao do shape do payload antes de confiar no `role`. A forca do segredo **ja foi paga** (cart, order, inventory e product recusam placeholder em qualquer ambiente e exigem 32+ caracteres em producao), e o `algorithms` explicito **ja existe no payment-service** desde o Bloco 3, junto da validacao de claims. Falta aplicar `algorithms` em auth, product, inventory e cart, e `issuer`/`audience` nos cinco. **Exposicao hoje:** o segredo e compartilhado, entao um token emitido para outro destino e aceito por qualquer servico — mas quem tem o segredo ja forja qualquer token, o mesmo raciocinio da excecao do token de servico ADMIN. Passa a importar quando existir identidade por servico, e a correcao e conjunta com escopos. (Levantado no quarto review do PR #52.)
- **`auth-service` sem validacao de ambiente no boot:** e o unico dos cinco que usam JWT sem modulo de config — le `process.env.JWT_SECRET as string` direto em `src/utils/jwt.ts`, sem checar presenca, placeholder ou tamanho. Com a variavel ausente, o `as string` entrega `undefined` ao `jwt.sign`, que falha em tempo de requisicao em vez de no boot. Aplicar a mesma regra dos outros quatro. (Descoberto ao tratar o achado de placeholder no PR de manutencao.)
- **403 genérico:** não vazar `required`/`current` no corpo (hoje alguns serviços retornam). Logar só server-side.
- **Autenticação serviço-a-serviço:** token interno/mTLS entre serviços (product→inventory, cart→product, order→inventory). Modelar identidade de serviço.
- **Separar `/health` (liveness) de `/ready` (readiness com check de DB):** quando houver health probes de orquestração.
- **Paginação no `/admin/users` (auth-service):** [herdada da Fase 2].
- **Portas de dev em 0.0.0.0:** postgres/mongo/redis publicam em todas as interfaces (rabbitmq já restrito a 127.0.0.1). Restringir + credenciais fortes por ambiente. Dev-only, baixa.
- **`deepmerge-ts` com stack exhaustion (GHSA-ggr8-5vv4-36mx), 3 avisos high no `npm audit`.** Cadeia unica confirmada por `npm ls deepmerge-ts`: `prisma@6.19.3` (devDependency, CLI) -> `@prisma/config` -> `deepmerge-ts@7.1.5`. O `@prisma/client`, que e dependencia de PRODUCAO, nao aparece na cadeia — nao ha exposicao em runtime, so em build/CLI. O `npm audit fix --force` faz downgrade para `prisma@6.12.0`, que e breaking. A mesma cadeia existe em TODOS os servicos da plataforma, entao a correcao e transversal e nao cabe num PR de feature. Reavaliar quando o Prisma publicar versao com `@prisma/config` corrigido.

### Infra / containerização
- **payment-service no docker-compose:** nasceu fora do compose (só `npm run dev`), mesmo estado do notification-service. Dockerfile, entrada no compose e healthcheck. Tratar junto com o item do notification-service, abaixo.
- **notification-service no docker-compose:** Dockerfile, entrada no compose, healthcheck e observabilidade (hoje só `npm run dev`).
- **Migração de args de fila durável (RabbitMQ):** args são imutáveis; adicionar a DLX exigiu deletar a fila (one-time manual). Padronizar via policy/quorum ou script de deploy.
- **Automatizar o e2e via docker-compose:** a suíte `e2e/` (Bloco 9) roda contra o stack local subido manualmente (auth, product, inventory, cart, order, notification + infra). Containerizar permite rodar no CI.
- **Limite de tamanho de mensagem no consumer:** `toString()+JSON.parse` sem checar `msg.content.length`. Cap de bytes + ACL/limite no broker. (Exchange interno, risco baixo hoje.)
- **Claim atômico entre instâncias do relay (`FOR UPDATE SKIP LOCKED`/lease):** hoje 1 instância; com múltiplas, dois relays publicariam o mesmo evento. Antes de escalar horizontalmente.
- **DLQ do notification — parking queue e classificação:** hoje payload inválido e falha de recuperação caem na mesma DLQ (distinção só no log). Follow-up: parking queue com atraso/metadados + classificação por header, para facilitar redrive e triagem.

### Resiliência
- **Timeout configurável + circuit breaker/retry no `inventory.client`** para falhas transitórias.
- **Validar config no boot do product-service:** garantir `INVENTORY_SERVICE_URL`; fallback localhost só em dev.
- **Logger estruturado:** substituir os `console.warn` dos clients por logger de verdade, com rate limit.
- **Cache stampede / single-flight (product-service):** requisições em cache frio vão todas ao banco; implementar single-flight + teste de concorrência.
- **Health do Redis (cart-service) com timeout por comando + circuit breaker** + teste de ping lento/pendente.

### Concorrência / atomicidade
- **setStock atômico (inventory):** guard (`quantity < reserved`) + upsert são separados (CHECK do banco é a rede atual). UPDATE condicional ou isolamento. Baixa (setStock é administrativo).
- **Corrida no cart (addItem/updateQuantity):** check-then-write não atômico + escrita e `EXPIRE` em comandos separados (janelas de corrida). MULTI/EXEC ou Lua + testes de concorrência. (PRs #33/#34.)

### Deploy
- **Índices MongoDB em produção** via script de migração/deploy (não auto-indexing do Mongoose).
- **Rate limit com store no Redis** compartilhado entre instâncias do auth ao escalar.

---

## FASE 10 — Resiliência avançada, jobs e qualidade

### Jobs / reconciliação

- **Retenção e minimização do inbox de webhooks (`payment-service`):** `webhook_events.payload` guarda o JSON íntegro do provedor, e `lastError`/`failureMessage` guardam texto arbitrário. Payload de gateway pode conter dados pessoais (nome, últimos dígitos, e-mail) e mensagens de erro podem carregar segredo. Definir campos permitidos, política de expiração e arquivamento. Tratar junto com a retenção da outbox. *(Item 3.2 da revisão do PR #47. A sanitização em escrita é critério de aceite dos Blocos 4 e 9, dentro da fase; aqui fica só a retenção.)*

- **Processor de reconciliação da saga:** o estado durável já existe (`idempotency_records` com orderId+status; `pending_compensations`). Falta o JOB, tratando cada caso sem liberar pedido válido:
  - `pending_compensations` aberta → reexecutar `release(orderId)` (idempotente) + marcar `resolvedAt`.
  - `idempotency_records` em `PROCESSING` → **bifurcar**: se o pedido existe, marcar `COMPLETED` (não liberar!); se não, `release` + `FAILED`.
  - `idempotency_records` `FAILED` → reexecutar `release(orderId)`.
  Tudo com retry/backoff, métricas e alerta.
- **Retenção/limpeza da outbox:** (ver o item de retenção do inbox, acima) `SENT`/`FAILED` crescem sem política; o payload guarda userId/total. Definir arquivamento/cleanup e minimizar campos.
- **Backoff/quarentena/redrive no relay da outbox (produtor):** o publish falho volta a `PENDING` e é retentado em intervalo fixo (sem backoff exponencial, sem quarentena de "poison" nem redrive) — um evento inpublicável causaria head-of-line blocking. Eventos são bem-formados (produtor próprio) → risco baixo hoje; necessário sob carga real. Enum `FAILED` reservado. (PR #43/#44.)
- **Limpeza do carrinho por versão/CAS:** hoje o checkout remove só os itens comprados por productId; um CAS por versão preservaria mudanças de quantidade. Exige versionamento no cart.

### Contratos / arquitetura
- **Contrato de topologia compartilhado:** EXCHANGE e routing keys duplicados em order (`events/topology.ts`) e notification (`config/topology.ts`). Pacote comum ou aceitar a duplicação.
- **Ciclo de vida acoplado a efeitos globais:** `start()`/estado do publisher rodam no import, com `process.exit` embutido e estado de módulo global. Separar construção/start/stop + injetar conexão/logger/exit.
- **Publisher e relay duplicados entre order-service e payment-service.** Era decisao consciente registrada quando so o order tinha; com o Bloco 5a virou fato consumado, e agora sao dois arquivos quase identicos que precisam evoluir juntos. Divergencia ja existe e e deliberada: o payment recebe a URL do broker por parametro (vinda do `loadConfig`) em vez de ler `process.env`. Avaliar pacote compartilhado quando um terceiro servico precisar publicar.
- **Knobs de tuning do publisher e do relay lidos direto de `process.env`** (`RABBITMQ_*_MS`, `OUTBOX_*`), fora do `loadConfig`. Inconsistencia interna consciente: sao valores sem relevancia de seguranca e com default seguro em faixa fechada, e trazer os oito para o `AppConfig` incharia a config sem ganho. Reavaliar se algum deles passar a ter efeito operacional relevante. **ATUALIZADO no 6c:** o gatilho registrado aqui ("reavaliar se algum passar a ter efeito operacional relevante") DISPAROU para os knobs das varreduras. `RECONCILIACAO_POLL_INTERVAL_MS`, `RECONCILIACAO_STOP_TIMEOUT_MS` e `JOBS_VARREDURA_TIMEOUT_MS` saem de `process.env` e entram no `AppConfig`, porque o primeiro participa de um invariante entre campos. Os do publisher e do relay continuam como estavam.

### Qualidade de testes / CI
- **Não existe CI neste repositório.** Levantado indiretamente no review do PR #52: o `statusCheckRollup` do PR vem **vazio**, e o revisor registrou por três vezes que "não é possível confirmar apenas com base no diff se os testes foram executados". Hoje a única evidência de que a bateria passou é o output colado na descrição do PR — que é palavra do autor, não verificação independente. Mínimo necessário: workflow rodando `npm run verify` e `npm run verify:integration` com Postgres de serviço, por serviço alterado.
- **CI prover env de teste:** `.env.test` não é versionado (`JWT_SECRET` etc.); o pipeline precisa setar, senão os testes de autorização falham. **O pipeline deve rodar `npm run verify`** (inclui type-check e build) **e `npm run verify:integration`** onde houver suíte separada — `npm test` sozinho não cobre build nem integração.
- **Teste de config do `redis.ts`** (fallback quando `REDIS_URL` ausente).
- **Teste de integração do `connectDatabase` (inventory):** `catch` sanitizado + `process.exit(1)`.
- **Fixar versão do MongoDB no `mongodb-memory-server` + cache no CI (product).**
- **Rodar `npm run verify` (build+test) no CI (product).**
- **Testes e2e com Supertest incl. 429 do rate limit** [herdada da Fase 2].
- **Teste de rollback quando a 2ª escrita da transação falha (order):** exige ponto de injeção de falha (a atomicidade vem do `$transaction`; o caminho "valida antes de escrever" já é testado).

---

## Refatoração transversal (→ Fase 7, pass de qualidade)
- **`PaymentService` concentra invariantes demais.** Levantado no review do PR #52 (achado 5.2): a classe reúne claim idempotente, autorização por posse, validação monetária, controle de concorrência, persistência, tradução de dependências, estado financeiro e composição da resposta. Não é observação estética — é o **diagnóstico da causa** dos achados 4.1, 4.2 e 4.5, que viviam nas interações entre esses invariantes e não dentro de nenhum deles. Separar a aquisição atômica da tentativa, a classificação do resultado do provedor e o armazenamento/replay idempotente em componentes menores. Fazer **depois** dos invariantes corrigidos, nunca junto.


- **Alinhar `database.ts` do inventory ao padrão do order:** exit centralizado no `server.ts`, não na camada de banco.
- **Erros de domínio como classes/enums** em vez de strings (inventory, product, cart).
- **Estender o tipo `Request` do Express** com usuário autenticado, eliminando `(req as any).userId/userRole`.
- **DTO explícito `ProductWithAvailability`** para o retorno enriquecido do `findProductById`.
- **Normalizar base URL do `inventory.client`** (`new URL()` ou tratar barra final).
- **Mover helpers puros para `utils/` (product):** `parsePositiveInt`, `pickAllowedFields`.
- **Precisão monetária:** representar dinheiro em centavos (inteiro), consistente entre serviços (hoje float com arredondamento em cart, product e order). **O payment-service JA usa centavos inteiros** (`src/domain/money.ts`) — não é alvo desta refatoração.

---

## Performance (→ carga real, Fase 7/10)

- **GET /cart faz N chamadas ao product (sem batch):** criar endpoint batch (`GET /products?ids=...`) e reduzir a uma chamada.

---

## Backlog de features (pós-MVP)

- **Login social via OAuth2 (Google):** [herdada da Fase 2] — sem fase definida.
