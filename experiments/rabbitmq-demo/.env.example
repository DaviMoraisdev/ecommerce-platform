# Ajuste a senha para bater com RABBITMQ_PASSWORD do .env da RAIZ do repo.
# Caracteres reservados na senha (@ : / #) precisam de percent-encoding na URL.
RABBITMQ_URL=amqp://admin:CHANGE_ME@localhost:5672
