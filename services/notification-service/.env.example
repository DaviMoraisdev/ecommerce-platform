RABBITMQ_URL=amqp://usuario:senha@127.0.0.1:5672
REDIS_URL=redis://:senha@127.0.0.1:6379

# Opcionais (defaults entre parenteses):
# IDEMPOTENCY_TTL_MS=172800000   # janela de dedup (48h; min 1min, max 7d)
# REQUEUE_DELAY_MS=1000          # atraso antes de requeue, anti hot loop (min 50ms)
