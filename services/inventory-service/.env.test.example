# Template das variaveis de ambiente para TESTES do inventory-service.
# Copie para .env.test e preencha. O .env.test e ignorado pelo git; este
# .example e versionado para documentar o que o ambiente de teste (e o CI) precisa.

# Banco de teste ISOLADO — deve conter 'test' no nome (salvaguarda do setup.ts).
DATABASE_URL=postgresql://postgres:senha@127.0.0.1:5432/inventory_test_db

INVENTORY_PORT=3004

# OBRIGATORIA e SEM VALOR PADRAO de proposito: um placeholder conhecido em
# repositorio publico e um segredo publico. Gere com:
#   openssl rand -hex 32
JWT_SECRET=

NODE_ENV=test
ALLOW_TEST_DB_RESET=true

# Exigido pela guarda em tests/helpers/testDbGuard.ts.
# ALLOW_TEST_DB_RESET=true e consentimento EXPLICITO para apagar este banco.
# ALLOW_REMOTE_TEST_DB desliga APENAS a checagem de host; o nome do banco
# continua obrigatorio. Nao habilite sem saber exatamente qual e o alvo.
# ALLOW_REMOTE_TEST_DB=true
